[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nkms"
description = "Network Keyboard Mouse Switch"
version = "0.2"
authors = [
  { name="Nicholas Yoder", email="nicholasyoder@proton.me" },
]

[tool.setuptools.packages.find]
include = ["nkms*"]
